export function RosewoodLogo({ className = "" }: { className?: string }) {
  return (
    <span
      className={`font-serif text-[1.6rem] leading-none tracking-[0.12em] uppercase text-forest ${className}`}
    >
      Rosewood
    </span>
  );
}
